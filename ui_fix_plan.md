# 《Bloom》UI整改方案

## 1. 手机端按钮遮挡问题修复

### 问题分析
- 对话框选项按钮位于页面底部
- 在小屏幕手机上可能被"结束今天"按钮或系统导航栏遮挡
- 需要确保所有交互元素在屏幕可见区域内

### 整改方案
1. **对话框选项区域上移**
   - 为 `.choices-area` 添加 `padding-bottom: 30px`
   - 在小屏幕（< 640px）时，选项区域增加额外的底部边距

2. **动态高度适配**
   - 限制对话框最大高度为屏幕的 70%
   - 启用内容区域自动滚动
   - 确保选项按钮始终可见

3. **触摸友好性优化**
   - 按钮最小高度提升至 48px
   - 增加点击区域，防止误触

## 2. 背景音乐和音效系统

### 设计目标
- 提供沉浸式游戏体验
- 音效可独立开关
- 支持音量调节
- 不干扰游戏流程

### 界面设计

#### 设置页面结构
```
┌─────────────────────────────┐
│  设置            ×         │
├─────────────────────────────┤
│  🎵 背景音乐                 │
│  ──────────── [ 开 ]        │
│                             │
│  🔊 音效音量                 │
│  ───────●─────────────      │
│      0%    50%    100%      │
│                             │
│  📝 打字音效                 │
│  ──────────── [ 开 ]        │
│                             │
│  👆 选择音效                 │
│  ──────────── [ 开 ]        │
│                             │
└─────────────────────────────┘
```

#### 设置按钮位置
- 在游戏主页右上角（日期旁边）添加设置图标
- 对话框右上角已有控制区域，可扩展为设置入口

## 3. 系统界面：存档/读档/设置

### 存档管理界面

#### 设计方案
- 支持 3 个存档槽位
- 每个槽位显示：存档时间、当前天数、攻略角色、好感度
- 快速存档/读档
- 支持删除存档

```
┌─────────────────────────────┐
│  存档管理          ×        │
├─────────────────────────────┤
│  槽位1                      │
│  📅 2026-04-05 21:00        │
│  Day 15 | 温雅 | 好感度 85   │
│  [读取]  [覆盖]  [删除]     │
│                             │
│  槽位2 [空]                 │
│  [存档]                     │
│                             │
│  槽位3 [空]                 │
│  [存档]                     │
└─────────────────────────────┘
```

#### 返回主菜单功能
- 在设置页面底部添加"返回主菜单"按钮
- 在游戏主页顶部栏添加返回按钮
- 首页已有"继续游戏"功能，增加"返回标题画面"选项

## 4. 帮助/教程系统

### MBTI测试引导

#### 引导弹窗设计
在进入MBTI测试前，显示引导弹窗：

```
┌─────────────────────────────┐
│  💡 游戏指南                │
├─────────────────────────────┤
│  欢迎来到《Bloom》！        │
│                             │
│  📝 了解自己的性格          │
│  选择最匹配的攻略对象        │
│                             │
│  🎮 如何游玩                │
│  • 每天有 3 次行动机会      │
│  • 选择活动提升好感度        │
│  • 做出选择影响剧情走向      │
│  • 目标：60天内攻略心仪对象  │
│                             │
│         [ 开始游戏 ]        │
└─────────────────────────────┘
```

#### 触发时机
- 首次进入游戏时自动显示
- 可在设置中重新查看

### 新手引导标记
- 在MBTI入口页添加提示气泡："了解你的性格，选择最匹配的攻略对象"
- 在角色选择页添加说明："选择你想攻略的她"

## 5. 页面过渡动画

### 过渡效果库
1. **淡入淡出**（Fade）
   - 用于页面切换
   - 时长：300ms
   - 缓动：ease-in-out

2. **滑入滑出**（Slide）
   - 用于对话框
   - 方向：从右向左（进入），从左向右（退出）
   - 时长：250ms

3. **缩放**（Scale）
   - 用于弹窗
   - 从 0.8 缩放到 1.0
   - 时长：200ms
   - 带弹性效果

4. **闪烁**（Pulse）
   - 用于高亮提示
   - 时长：500ms

### 实现方式
- 使用 CSS Transitions 和 Animations
- 为每个 `.page` 添加过渡类
- JavaScript 控制类名切换

## 6. 额外UI优化

### 好感度进度条优化
- 当前：只显示数字
- 优化：增加彩色进度条
  - 0-30%: 灰色（陌生）
  - 30-50%: 蓝色（朋友）
  - 50-70%: 紫色（暧昧）
  - 70-100%: 粉色（恋人）
- 动态颜色过渡

### 角色解锁动画
- 第一次遇到角色时：
  1. 角色立绘从底部滑入
  2. 显示角色信息卡片
  3. 卡片包含：名字、性格、一句话介绍
  4. 动画时长：500ms

### 探索地点视觉优化
- 6个地点使用不同图标
- 未探索地点显示"？"图标
- 已探索地点显示实际场景缩略图
- 当前可选地点添加高亮边框

### 对话框优化
- 打字机效果可点击加速
- 角色头像在说话时轻微晃动（scale: 1.05）
- 对话气泡添加打字光标动画

## 7. 响应式设计

### 断点设置
- 手机：< 640px
- 平板：640px - 1024px
- 桌面：> 1024px

### 手机端特殊适配
1. **字体大小**
   - 基础字号：14px（手机）、16px（桌面）
   - 标题字号按比例调整

2. **间距调整**
   - 按钮padding：10px 15px（手机）
   - 卡片间距：10px（手机）、15px（桌面）

3. **触摸目标**
   - 最小点击区域：44px × 44px
   - 按钮高度：至少 48px

4. **键盘适配**
   - 对话框选项区域上移 30px
   - 避免被虚拟键盘遮挡

## 8. 交互反馈优化

### 按钮点击反馈
1. **视觉反馈**
   - 点击瞬间：scale(0.95)
   - 恢复：scale(1.0)
   - 时长：150ms

2. **声音反馈**
   - 点击音效
   - 可在设置中关闭

3. **状态变化**
   - 禁用状态：opacity: 0.5
   - 加载状态：显示旋转动画

### 提示信息优化
- Toast 提示时长：2秒
- 关系升级提示：3秒
- 错误提示：2.5秒，红色背景

## 9. 实现优先级

### P0（立即修复）
1. 手机端按钮遮挡问题
2. 添加设置页面
3. 添加存档管理界面

### P1（高优先级）
4. 添加新手引导弹窗
5. 背景音乐和音效系统（UI部分）
6. 页面过渡动画

### P2（中优先级）
7. 好感度进度条优化
8. 角色解锁动画
9. 探索地点视觉优化

### P3（低优先级）
10. 对话框细节优化
11. 高级交互反馈

## 10. 技术实现要点

### CSS架构
- 使用 CSS 变量管理主题色
- 使用 Flexbox 和 Grid 布局
- 使用媒体查询实现响应式

### JavaScript要点
- 使用类名控制页面切换
- 使用 localStorage 存储设置
- 使用 CSS Animation API 控制动画

### 兼容性
- 支持 iOS Safari
- 支持 Android Chrome
- 支持桌面主流浏览器

## 11. 可访问性

### 键盘导航
- 所有按钮可通过 Tab 键聚焦
- 使用 Enter/Space 键触发

### 屏幕阅读器
- 为按钮添加 aria-label
- 为动态内容添加 aria-live

### 对比度
- 文本对比度至少 4.5:1
- 重要按钮对比度至少 3:1
