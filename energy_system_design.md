# 《Bloom》精力机制设计文档

## 一、方案评估

### 方案A：每天3点精力值（推荐）

**设计思路**：
- 每进行一个活动消耗1点精力
- 每天精力重置为3点
- 精力耗尽后强制进入下一天

**优点**：
1. 简单直接，玩家容易理解
2. 约束力强，每个选择都重要
3. 不会因为计算复杂而让玩家困惑
4. 每天固定3次互动，节奏可控

**缺点**：
1. 缺乏策略深度（所有活动消耗相同）
2. 可能导致玩家重复刷固定路线

---

### 方案B：精力100，每天消耗不同

**设计思路**：
- 精力值100点，每天不回复
- 不同活动消耗不同精力（5-30点）
- 可以进行更多低消耗活动或少量高消耗活动

**优点**：
1. 有策略性，玩家需要权衡
2. 可以设计更多活动类型
3. 高消耗活动可能有更高奖励

**缺点**：
1. 计算复杂
2. 精力约束力弱（100点太多）
3. 可能导致玩家反复计算最优解

---

## 二、最终选择：方案A

**选择理由**：
1. 简单性优先：休闲游戏的玩家不喜欢复杂计算
2. 约束力足够：每天3次机会已经足够做出有意义的选择
3. 节奏控制：60天×3次 = 180次互动，平均每条线36次
4. 适合移动端：操作简单，反馈清晰

---

## 三、精简精力系统设计

### 核心规则

```
每天精力：3点
每活动消耗：1点
精力耗尽：强制进入下一天
精力刷新：每天开始时重置为3点
不累积：不使用的精力不会累积到下一天
```

### UI展示

```
┌────────────────────────────────────┐
│ ☀️ Day 5          ⚡⚡⚡ 3/3 精力  │
└────────────────────────────────────┘
```

- 用3个⚡符号表示当前精力
- 有精力时显示实心符号
- 无精力时显示空心符号

### 活动设计

所有活动统一消耗1点精力：

| 活动类型 | 活动名称 | 精力消耗 | 说明 |
|----------|----------|----------|------|
| 社交 | 健身房 | 1点 | 可能遇到夏阳、陆潇 |
| 社交 | 梧桐公园 | 1点 | 可能遇到苏静怡、温雅 |
| 社交 | 咖啡店 | 1点 | 可能遇到陆潇、苏静怡 |
| 社交 | 法餐厅 | 1点 | 陆潇专属场景 |
| 社交 | 画廊 | 1点 | 温雅专属场景 |
| 社交 | 医院门口 | 1点 | 苏静怡专属场景 |
| 社交 | 写字楼大堂 | 1点 | 林婉清专属场景 |
| 休息 | 便利店 | 0点 | 恢复精力，可能遇到随机事件 |

---

## 四、退出惩罚机制

### 当前问题
- 可以随意跳过对话，没有任何后果
- 破坏了游戏的紧张感和选择的重要性

### 整改方案

#### 1. 退出确认弹窗

```
┌────────────────────────────────────────┐
│          ⚠️ 确定要离开吗？              │
│                                        │
│    [角色名] 可能会失望……               │
│                                        │
│    如果现在离开，好感度将下降。          │
│                                        │
│         [继续对话]    [确认离开]         │
└────────────────────────────────────────┘
```

#### 2. 退出后果

| 退出时机 | 好感度变化 | 说明 |
|----------|------------|------|
| 对话中途退出 | -10 | 中断互动 |
| 选择前退出 | -15 | 逃避选择 |
| 连续退出 | -20 | 严重后果 |

#### 3. 连续退出事件

| 连续退出次数 | 触发事件 | 效果 |
|--------------|----------|------|
| 2次 | 角色发消息询问 | 好感度-5，角色主动联系 |
| 3次 | 角色生气 | 好感度-15，进入疏远状态 |
| 4次+ | 关系恶化 | 可能触发BE结局 |

#### 4. 角色退出反应

| 角色 | 退出反应 | 疏远表现 |
|------|----------|----------|
| 温雅 | "……没关系，我知道你需要空间。" | 不再主动送曲奇，减少微笑 |
| 夏阳 | "啊？要走了？好吧……下次见！" | 笑容变淡，不再主动搭话 |
| 苏静怡 | "……好。注意休息。" | 很少回复消息 |
| 陆潇 | "哦？那下次见。" | 不会再特别招待 |
| 林婉清 | "看来你很忙。" | 公事公办，不再私下交流 |

---

## 五、实现说明

### 代码结构

```javascript
class GameState {
    constructor() {
        this.day = 1;
        this.maxDay = 60;
        this.energy = 3;      // 每天3点精力
        this.maxEnergy = 3;
        this.continueExitCount = 0;  // 连续退出计数
    }
    
    useEnergy() {
        if (this.energy > 0) {
            this.energy--;
            return true;
        }
        return false;
    }
    
    resetDaily() {
        this.energy = this.maxEnergy;
    }
}

class GameEngine {
    // 退出确认
    showExitConfirm() {
        // 显示确认弹窗
        // 如果确认退出，调用 handleExit()
    }
    
    handleExit() {
        const charId = this.state.selectedChar;
        
        // 更新连续退出计数
        this.state.continueExitCount++;
        
        // 计算惩罚
        let penalty = 10;
        if (this.state.continueExitCount >= 3) {
            penalty = 20;
        }
        
        // 应用惩罚
        this.state.charAttitudes[charId] -= penalty;
        
        // 显示角色反应
        const reaction = EXIT_REACTIONS[charId];
        if (this.state.continueExitCount >= 2) {
            this.showExitReaction(reaction.follow_up[this.state.continueExitCount]);
        } else {
            this.showExitReaction(reaction);
        }
        
        // 关闭对话框
        this.closeDialogue();
    }
    
    makeChoice(idx) {
        // 重置连续退出计数
        this.state.continueExitCount = 0;
        // ... 正常选择逻辑
    }
}
```

---

## 六、测试要点

1. **精力耗尽测试**：验证精力为0时无法进行活动
2. **精力重置测试**：验证进入新天后精力重置为3
3. **退出惩罚测试**：验证不同退出场景的好感度变化
4. **连续退出测试**：验证连续退出触发特殊事件
5. **UI显示测试**：验证精力点显示正确
